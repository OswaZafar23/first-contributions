# My GitHub Contribution Process

This project documents the steps I followed to contribute to this repository:

1. Created a `screenshots` folder in my local system.
2. Copied the folder into the Git repository.
3. Added and committed the folder using Git:
   ```bash
   git add screenshots
   git commit -m "Added screenshots folder with images"
   git push origin add-OswaZafar23
